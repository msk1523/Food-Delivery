specVersion: "v1beta"
serviceId: "food_delivery"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "food_delivery-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
